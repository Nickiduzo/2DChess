using UnityEngine;

[RequireComponent(typeof(Board))]
public class FigureTranslation : MonoBehaviour
{
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
